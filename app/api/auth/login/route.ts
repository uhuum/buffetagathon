import{NextResponse}from'next/server'
import{createClient}from'@/lib/supabase/server'
const LOGIN_EMAIL='edna@agathon.local'
export async function POST(r:Request){const{usuario,senha}=await r.json();if(typeof usuario!=='string'||typeof senha!=='string')return NextResponse.json({message:'Usuário e senha são obrigatórios'},{status:400});if(usuario.trim().toLowerCase()!=='edna')return NextResponse.json({message:'Usuário ou senha inválidos'},{status:401});const supabase=await createClient();const{error}=await supabase.auth.signInWithPassword({email:LOGIN_EMAIL,password:senha});if(error)return NextResponse.json({message:'Usuário ou senha inválidos'},{status:401});return NextResponse.json({success:true})}